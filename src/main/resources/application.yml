spring:
    application:
        name: order-service
    jpa:
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                default_schema: order_service
                hbm2ddl.create_namespaces: true # 스키마 자동 생성

server:
    port: 19700
